[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: fast isolated tests
    api: API contract and endpoint behavior tests
    integration: database/pipeline integration tests
    quality: data-quality assertion tests
    e2e: end-to-end tests across service boundaries
